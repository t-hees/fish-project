server.port=${BACKEND_PORT}

spring.application.name=fish-project
spring.datasource.url=jdbc:mariadb://${DB_HOST}:${DB_PORT}/${DB_DBNAME}?createDatabaseIfNotExist=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.initialization-fail-timeout=30000
spring.jpa.hibernate.ddl-auto=update

spring.servlet.multipart.max-file-size=5MB

spring.jpa.show-sql=true

jwt.secret=${JWT_SECRET}
# Note: enabling the following prevents the actual table creation!
#spring.jpa.properties.jakarta.persistence.schema-generation.scripts.action=create
#spring.jpa.properties.jakarta.persistence.schema-generation.scripts.create-target=src/main/resources/schema.sql